// Public, non-secret site configuration. Google Client IDs are safe to expose here.
window.CLASS_NOTEBOOK_CONFIG = {
  // Replace with the Web OAuth client ID from Google Cloud Console.
  googleClientId: '',
};
